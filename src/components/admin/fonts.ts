import { Plus_Jakarta_Sans } from "next/font/google";

// Headlines del admin/portal: Plus Jakarta Sans 800, como especifica
// Brand Kit v1 (sección 03 · typography) — no Anton (--font-title es del
// sitio de marketing, un sistema tipográfico distinto).
export const jakarta = Plus_Jakarta_Sans({ subsets: ["latin"], weight: "800" });
